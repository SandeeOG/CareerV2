# Copy to .env and fill in to enable the real Gemini-powered AI coach.
# Get a free key: https://aistudio.google.com/app/apikey
# Without this set, the app runs fully offline with a deterministic mentor.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

DM_HOST=0.0.0.0
DM_PORT=8000
