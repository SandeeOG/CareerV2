"""Detective Monkey v2 — Career Intelligence Platform.

This package contains the **domain foundation** produced in Phases P0 (Foundation)
and P1 (Domain). It deliberately contains:

    - NO database / ORM
    - NO API / web framework
    - NO AI provider integration
    - NO scoring/engine algorithms (those are Phase 2)

It contains only the canonical, framework-independent domain model and the
contracts (interfaces) through which future engines cooperate.

See ``README.md`` for the mapping between each module and its source design
document under ``P0/`` and ``P1/``.
"""

__version__ = "2.0.0"
